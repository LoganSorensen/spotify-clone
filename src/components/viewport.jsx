import React from "react";

import TopBar from "./topBar";

const Viewport = () => {
  return (
    <div className="viewport">
      <TopBar />
      <div>Viewport</div>
    </div>
  );
};

export default Viewport;
